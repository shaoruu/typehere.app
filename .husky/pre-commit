#!/usr/bin/env sh
. "$(dirname -- "$0")/_/h"

# Load nvm (if available) and use the correct Node.js version
export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use 20 >/dev/null
fi

# Run lint-staged against staged files
npx --no-install lint-staged
